# Issue Fix Log

## 2026-05-24 - Home 화면이 비어 보임

### 현상
`/ko` 또는 `/?nav=home` 진입 시 DOM에는 홈 콘텐츠가 존재하지만, 실제 화면에는 흰 배경과 로그인 링크만 보이고 본문이 거의 보이지 않았다.

### 원인
홈 화면 본문이 `framer-motion`의 초기 `opacity: 0` 애니메이션 상태에 의존하고 있었다. 클라이언트 애니메이션 적용이 지연되거나 꼬이면 본문이 투명 상태로 남아, 화면이 비어 보이는 회귀가 발생했다. 또한 홈 진입 시 백엔드 사전 확인 요청을 기다리는 구조가 있어 사용자가 빈 화면으로 느끼는 시간이 길어졌다.

### 해결방안
홈 본문 렌더링을 애니메이션 상태에 의존하지 않도록 일반 `div` 렌더링으로 복구했다. 홈 사전 확인 요청은 타임아웃을 두고 백그라운드에서만 처리되게 바꿔, 홈 콘텐츠가 먼저 보이도록 했다.

## 2026-05-24 - 퀴즈 답변 버튼 클릭 후 다음 문항으로 넘어가지 않음

### 현상
`/ko/onboarding/quiz`에서 답변 버튼을 누르면 선택 색상은 바뀌지만 `1 / 7` 상태에 머물고 다음 문항으로 진행되지 않았다.

### 원인
홈/로그인/보관함 회귀를 수정하는 과정에서 실제 온보딩 퀴즈의 `답변 클릭 -> 다음 문항 -> 결과 페이지` 흐름까지 끝까지 검증하지 않았다. 기존 기능 보존 관점에서 필요한 사용자 플로우 회귀 테스트가 빠졌고, 로컬 개발 서버가 이전 클라이언트 번들을 계속 잡고 있어 코드 수정 여부와 실제 화면 상태가 어긋났다.

### 해결방안
퀴즈 진행을 로컬 드래프트 저장 성공 여부와 타이머 상태에 의존하지 않도록 단순화했다. 드래프트 저장은 best-effort로 처리하고, 답변 선택 후 상태 전환은 즉시 실행되게 했다. dev 서버를 재시작한 뒤 `/ko/onboarding/quiz`에서 실제 답변 클릭으로 `2 / 7` 진행 여부를 확인했다.

## 2026-05-24 - 퀴즈 선택값을 디버그 모드에서 확인하기 어려움

### 현상
`/ko/onboarding/quiz`에서 사용자가 어떤 답변을 선택했고 그 값이 어떤 persona/answerIndices로 저장되는지 화면만 보고는 확인하기 어려웠다.

### 원인
퀴즈 선택 이벤트가 analytics와 로컬 드래프트 저장으로만 이어지고, `NEXT_PUBLIC_DEBUG_MODE=1`일 때 확인 가능한 개발자 콘솔 로그가 없었다.

### 해결방안
디버그 모드에서만 `[NNAI quiz]` 콘솔 로그를 출력하도록 추가했다. 첫 줄에는 질문 번호, 선택한 답변 label, persona, 다음 이동 위치가 바로 보이고, 펼친 객체에는 질문 텍스트, 선택한 답변 index/label, persona, 다음 index, 저장될 answers/answerIndices가 포함된다. Chrome DevTools 기본 Log level에서도 보이도록 `console.info`를 사용하고, 일반 모드에서는 로그와 화면 동작이 바뀌지 않는다.

## 2026-05-24 - 노마드 유형 완료 후 홈에서 다시 퀴즈로 진입함

### 현상
사용자가 설문으로 `persona_type`을 이미 정한 뒤에도 홈 화면의 기본 CTA를 누르면 노마드 유형 퀴즈로 다시 이동할 수 있었다.

### 원인
홈 화면 CTA가 저장된 `persona_type` 여부와 무관하게 항상 `/onboarding/quiz`로 연결되어 있었다. 또한 `/onboarding/quiz` 페이지 자체에도 이미 유형을 완료한 사용자를 걸러내는 진입 방어가 없었다.

### 해결방안
홈 화면에서 `localStorage.persona_type`이 있으면 기본 CTA를 `/onboarding/form`으로 연결하고, 퀴즈 페이지에 직접 접근해도 저장된 유형이 있으면 `/onboarding/form`으로 리다이렉트하도록 했다. 유형을 한 번도 완료하지 않은 사용자만 퀴즈 페이지로 들어간다.

## 2026-05-24 - 보관함 카드 도시명 오른쪽 국기 누락

### 현상
보관함 카드에서 쿠알라룸푸르처럼 도시명 오른쪽에 국기가 보여야 하는데 일부 카드에서 국기가 보이지 않거나 뒤로 밀려 보일 수 있었다.

### 원인
도시 데이터의 `country_id`는 `MY`로 정상이었다. 문제는 보고서 카드가 비교 버튼 공간을 확보하려고 제목 영역 오른쪽에 `pr-7`을 적용해, 쿠알라룸푸르처럼 긴 도시명이 들어갈 때 도시명과 국기가 쓸 수 있는 가로 폭이 부족해지는 구조였다.

### 해결방안
도시명과 국기는 기존처럼 같은 텍스트 흐름에 두고, 비교 버튼용 오른쪽 여백만 `pr-7`에서 `pr-5`로 줄였다. 카드 버튼과 제목이 겹치지 않으면서 쿠알라룸푸르 글자가 잘릴 가능성을 낮췄다.

## 운영 원칙

앞으로 버그 수정 시 이 파일에 항상 `현상`, `원인`, `해결방안` 순서로 간단히 기록한다. 화면/UI 관련 수정은 해당 화면의 기존 주요 사용자 플로우를 실제 브라우저에서 최소 1회 확인한다.
